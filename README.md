## Backend MERN - Calendar

## Dev

1. Clonar repositorio
2. Instalar node modulos con `npm install`
3. hacer la conexión de base de datos copiando el `.env.template` y renombrando a solo `.env`, cambiar la liga de conexión de base de datos y palabra secreta
4. Correr el servidor `npm start`
